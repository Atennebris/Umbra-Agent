version: "3.9"

services:
  daemon:
    image: umbra-daemon:latest
    ports:
      - "4200:4200"
    volumes:
      - umbra_data:/root/.umbra

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: umbra
      POSTGRES_USER: admin

volumes:
  umbra_data:
